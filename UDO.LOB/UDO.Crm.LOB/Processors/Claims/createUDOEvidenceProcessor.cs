using Microsoft.Xrm.Sdk;
using Microsoft.Xrm.Sdk.Client;
using Microsoft.Xrm.Sdk.Messages;
using System;
using System.Linq;
using VIMT.TrackedItemServiceRemote.Messages;
using VRM.Integration.Servicebus.Core;
using VRM.Integration.Servicebus.Logging.CRM.Util;
using VRM.Integration.UDO.Claims.Messages;
using VRM.Integration.UDO.Common;
using Logger = VRM.Integration.Servicebus.Core.Logger;

/// <summary>
/// VIMT LOB Component for UDOcreateUDOEvidence,createUDOEvidence method, Processor.
/// Code Generated by IMS on: 6/1/2015 1:36:00 PM
/// Version: 2015.05.05
/// </summary>
/// <param name=none></param>
/// <returns>none</returns>
namespace VRM.Integration.UDO.Claims.Processors
{
    class UDOcreateUDOEvidenceProcessor
    {
        private bool _debug { get; set; }
        private const string method = "UDOcreateUDOEvidenceProcessor";
        private string LogBuffer { get; set; }

        public IMessageBase Execute(UDOcreateUDOEvidenceRequest request)
        {
            //var request = message as createUDOEvidenceRequest;
            UDOcreateUDOEvidenceResponse response = new UDOcreateUDOEvidenceResponse();
            var progressString = "Top of Processor";
            LogBuffer = string.Empty;
            _debug = request.Debug;

            if (request == null)
            {
                response.ExceptionMessage = "Called with no message";
                response.ExceptionOccured = true;
                return response;
            }

            Logger.Instance.Info(string.Format("Message Id:{0}, Type={2}, Recieved diagnostics message: {1}",
            request.MessageId,
            request.MessageId,
            GetType().FullName));

            OrganizationServiceProxy OrgServiceProxy;

            #region connect to CRM
            try
            {
                var CommonFunctions = new CRMConnect();

                OrgServiceProxy = CommonFunctions.ConnectToCrm(request.OrganizationName);

            }
            catch (Exception connectException)
            {
                LogHelper.LogError(request.OrganizationName, request.UserId, string.Format("{0} Processor, Connection Error", method), connectException.Message);                
                response.ExceptionMessage = "Failed to get CRMConnection";
                response.ExceptionOccured = true;
                return response;
            }
            #endregion

            progressString = "After Connection";
            var requestCollection = new OrganizationRequestCollection();

            try
            {
                // prefix = findUnsolEvdnceRequest();
                var findUnsolEvdnceRequest = new VIMTfindUnsolEvdnceRequest();
                findUnsolEvdnceRequest.LogTiming = request.LogTiming;
                findUnsolEvdnceRequest.LogSoap = request.LogSoap;
                findUnsolEvdnceRequest.Debug = request.Debug;
                findUnsolEvdnceRequest.RelatedParentEntityName = request.RelatedParentEntityName;
                findUnsolEvdnceRequest.RelatedParentFieldName = request.RelatedParentFieldName;
                findUnsolEvdnceRequest.RelatedParentId = request.RelatedParentId;
                findUnsolEvdnceRequest.UserId = request.UserId;
                findUnsolEvdnceRequest.OrganizationName = request.OrganizationName;

                findUnsolEvdnceRequest.mcs_claiment_ptpcpnt_id = request.Claiment_ptpcpnt_id;
                findUnsolEvdnceRequest.LegacyServiceHeaderInfo = new VIMT.TrackedItemServiceRemote.Messages.HeaderInfo
                {
                    ApplicationName = request.LegacyServiceHeaderInfo.ApplicationName,
                    ClientMachine = request.LegacyServiceHeaderInfo.ClientMachine,
                    LoginName = request.LegacyServiceHeaderInfo.LoginName,
                    
                    StationNumber = request.LegacyServiceHeaderInfo.StationNumber
                };
                // TODO(TN): Comment to remediate
                var findUnsolEvdnceResponse = new VIMTfindUnsolEvdnceResponse();
                // var findUnsolEvdnceResponse = findUnsolEvdnceRequest.SendReceive<VIMTfindUnsolEvdnceResponse>(MessageProcessType.Local);
                progressString = "After VIMT EC Call";

                response.ExceptionMessage = findUnsolEvdnceResponse.ExceptionMessage;
                response.ExceptionOccured = findUnsolEvdnceResponse.ExceptionOccured;
                if (findUnsolEvdnceResponse.VIMTUnsolicitedEvidencetrkItemInfo != null)
                {
                    var UnsolicitedEvidence = findUnsolEvdnceResponse.VIMTUnsolicitedEvidencetrkItemInfo;
                    System.Collections.Generic.List<UDOcreateUDOEvidenceMultipleResponse> UDOcreateUDOEvidenceArray = new System.Collections.Generic.List<UDOcreateUDOEvidenceMultipleResponse>();
                    foreach (var UnsolicitedEvidenceItem in UnsolicitedEvidence)
                    {
                        var responseIds = new UDOcreateUDOEvidenceMultipleResponse();
                        //instantiate the new Entity
                        Entity thisNewEntity = new Entity();
                        thisNewEntity.LogicalName = "udo_evidence";
                        if (request.ownerId != System.Guid.Empty)
                        {
                            thisNewEntity["ownerid"] = new EntityReference(request.ownerType, request.ownerId);
                        }

                        if (UnsolicitedEvidenceItem.mcs_userName != string.Empty)
                        {
                            thisNewEntity["udo_lastupdatedby"] = UnsolicitedEvidenceItem.mcs_userName;
                        }
                        if (UnsolicitedEvidenceItem.mcs_jrnDt != System.DateTime.MinValue)
                        {
                            thisNewEntity["udo_journaldate"] = UnsolicitedEvidenceItem.mcs_jrnDt;
                        }
                        if (UnsolicitedEvidenceItem.mcs_rcvdDt != System.DateTime.MinValue)
                        {
                            thisNewEntity["udo_datereceived"] = UnsolicitedEvidenceItem.mcs_rcvdDt;
                        }
                        if (UnsolicitedEvidenceItem.mcs_descTxt != string.Empty)
                        {
                            thisNewEntity["udo_name"] = UnsolicitedEvidenceItem.mcs_descTxt;
                        }
                        if (request.UDOcreateUDOEvidenceRelatedEntitiesInfo != null)
                        {
                            foreach (var relatedItem in request.UDOcreateUDOEvidenceRelatedEntitiesInfo)
                            {
                                thisNewEntity[relatedItem.RelatedEntityFieldName] = new EntityReference(relatedItem.RelatedEntityName, relatedItem.RelatedEntityId);
                            }
                        }
                        //responseIds.newUDOcreateUDOEvidenceId = OrgServiceProxy.Create(TruncateHelper.TruncateFields(thisNewEntity, request.OrganizationName, request.UserId, request.LogTiming));
                        //UDOcreateUDOEvidenceArray.Add(responseIds);
                        CreateRequest createData = new CreateRequest
                        {
                            Target = thisNewEntity
                        };
                        requestCollection.Add(createData);
                    }
                    if (requestCollection.Count() > 0)
                    {
                        var result = ExecuteMultipleHelper.ExecuteMultiple(OrgServiceProxy, requestCollection, request.OrganizationName, request.UserId, request.Debug);

                        if (_debug)
                        {
                            LogBuffer += result.LogDetail;
                            LogHelper.LogDebug(request.OrganizationName, _debug, request.UserId, method, LogBuffer);
                        }

                        if (result.IsFaulted)
                        {
                            LogHelper.LogError(request.OrganizationName, request.UserId, method, result.ErrorDetail);
                            response.ExceptionMessage = result.FriendlyDetail;
                            response.ExceptionOccured = true;
                            return response;
                        }
                    }

                    #region Log Results
                    string logInfo = string.Format("Evidence Records Created: {0}", requestCollection.Count());
                    LogHelper.LogInfo(request.OrganizationName, request.Debug, request.UserId, "Evidence Records Created", logInfo);
                    #endregion
                }

                //added to generated code
                if (request.udo_claimId != System.Guid.Empty)
                {
                    var parent = new Entity();
                    parent.Id = request.udo_claimId;
                    parent.LogicalName = "udo_claim";
                    parent["udo_evidencecomplete"] = true;
                    //parent["udo_evidencemessage"] = "";
                    OrgServiceProxy.Update(TruncateHelper.TruncateFields(parent, request.OrganizationName, request.UserId, request.LogTiming));
                }
                return response;
            }
            catch (Exception ExecutionException)
            {
                LogHelper.LogError(request.OrganizationName, request.UserId, request.RelatedParentId, request.RelatedParentEntityName, request.RelatedParentFieldName, method + " Processor, Progess:" + progressString, ExecutionException);
                response.ExceptionMessage = "Failed to process Evidence data";
                response.ExceptionOccured = true;
                return response;
            }
        }
    }
}