describe('VIP.view.Viewport', function () {
    var viewport = null;

    beforeEach(function () {

    });

    it('should define a search overview header', function () {
        //expect(viewport.getComponent('searchoverview')).toBeDefined();
    });

    it('should define a central content pane', function () {
        //expect(viewport.getComponent('contentpanel')).toBeDefined();
    });

    it('should define a status bar footer', function () {
        //expect(viewport.getComponent('appstatus')).toBeDefined();
    });
});