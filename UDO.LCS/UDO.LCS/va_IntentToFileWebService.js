var ws = ws || {};
ws.intentToFile = {};
// Function will be called by form onLoad event
ws.intentToFile.initalize = function () {
// CSDev Left Intentionally Blank 
};

// CSDev Left Intentionally Blank function NN(s) { return (s == null ? '' : s); }