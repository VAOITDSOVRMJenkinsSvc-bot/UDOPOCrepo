var silverlightControl,
    emergencyOptions = window.opener._emergencyOptions,
    emergencyType,
    recipients = [],
    message = '',
    button;

function onPluginLoaded(sender, args) {
  // CSDev Left Intentionally Blank 
}

function load() {
 // CSDev Left Intentionally Blank 
}

function addEvent(evnt, elem, func) {
// CSDev Left Intentionally Blank 
}

function getRecipients() {
// CSDev Left Intentionally Blank 
}

function setSelection(element) {
// CSDev Left Intentionally Blank 
}

function setEmergencyType() {
// CSDev Left Intentionally Blank 
}

function getCancelEmergencyMesssage() {
   // CSDev Left Intentionally Blank  return 'The ' + (emergencyType.value === 4 ? '' : emergencyType.name.toLowerCase()) + ' emergency was canceled!'
}

function setNewRecipients() {
  // CSDev Left Intentionally Blank   document.getElementById("addRecipients").value = emergencyOptions.newRecipients;
}

function getMessage(value) {
// CSDev Left Intentionally Blank 
}

function setMessage(value) {
// CSDev Left Intentionally Blank 
}

function disableAll() {
// CSDev Left Intentionally Blank 
}

function onSilverlightError(sender, args) {
// CSDev Left Intentionally Blank 
}

function onLyncErrors(message) {
// CSDev Left Intentionally Blank 
}

function onLyncExecuteReady() {
// CSDev Left Intentionally Blank 
}



