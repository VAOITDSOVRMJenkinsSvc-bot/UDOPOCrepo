var Va = Va || {};
Va.Udo = Va.Udo || {};
Va.Udo.Crm = Va.Udo.Crm || {};
Va.Udo.Crm.Scripts = Va.Udo.Crm.Scripts || {};
Va.Udo.Crm.Scripts.ClaimsDocumentDownload = Va.Udo.Crm.Scripts.ClaimsDocumentDownload || {};

function ClaimOnLoad() {
    // CSDev Left Intentionally Blank 
}

function ClaimStatusTabChange() {
    // CSDev Left Intentionally Blank 
}

function ClaimSuspenseTabChange() {
    // CSDev Left Intentionally Blank 
}

function genericTabChange(tabName, sectionName) {
    // CSDev Left Intentionally Blank 
}

function ClaimDetailTabChange() {
    // CSDev Left Intentionally Blank 
}

function ContentionsTabChange() {
    // CSDev Left Intentionally Blank 
}

function LifeCycleTabChange() {
    // CSDev Left Intentionally Blank 
}

function TrackedItemsTabChange() {
    // CSDev Left Intentionally Blank 
}

function EvidenceTabChange() {
    // CSDev Left Intentionally Blank 
}

function MapDTabChange() {
    // CSDev Left Intentionally Blank 
}

function GetSelectedMapDRecord() {

// CSDev Left Intentionally Blank 
}

function retrieveClaimDocument(id) {
// CSDev Left Intentionally Blank 
}

function executeAction(entityId, entityName, requestName) {
// CSDev Left Intentionally Blank 
}

function onResponse(responseObject, requestName) {
// CSDev Left Intentionally Blank 
}

function retrieveAnnotation(claimdocid) {
// CSDev Left Intentionally Blank 
}

function buildQueryFilter(field, value, and) {
  // CSDev Left Intentionally Blank 
}

function OverrideDoubleClickMAPDLettersGrid() {
// CSDev Left Intentionally Blank 
}

// download: download a base64 file
//
// fileName: the default name of the file
// mimeType: the mime type of the file
// fileContents: base64 string containing the base64 file contents (without the base64; prefix)
function download(fileName, mimeType, fileContents) {

// CSDev Left Intentionally Blank 
}


function SetFocusOn1stField(myTab, mySection) {
  // CSDev Left Intentionally Blank 
}

function USDCreateLetter(personid, data) {
// CSDev Left Intentionally Blank 
}
function USDCreateServiceRequest(personid, data) {
// CSDev Left Intentionally Blank 
}

function USDCreateLetterOrServiceRequest(personid, targetEntity, messageName, data) {
// CSDev Left Intentionally Blank 
}
